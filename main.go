package main

func main() {
	// 2 tab interface - mapping with sample editor / settings for sweep folder
	// sweep workflow will produce a text file with values
	// SQLite table to hold the saved xml path to whatever its mapping to
	// Create a 'New' button that creates another DD row (allow input to give it a name)
	// Create Database function
}
